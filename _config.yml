plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag  
  
theme: minima

title: The Osvalds
author: Nikolas Osvalds
email: nik@osvalds.net
# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >
  The Osvalds Family blog.

facebook_username: nosvalds
instagram_username: nosvalds
linkedin_username: nosvalds